import React from 'react';
import { Link } from 'react-router';

const Heasder = () => {
    return (
        <div>
            <Link to="/cards" >Cards</Link>
        </div>
    );
};

export default Heasder;